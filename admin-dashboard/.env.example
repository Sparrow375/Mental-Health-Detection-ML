# ── Frontend (Vite) ──────────────────────────────────────────────────
# HF Space base URL used by the React app (VoiceAssessment.tsx).
# Format: https://YOUR_HF_USERNAME-mhealth-voice-api.hf.space
VITE_HF_API_URL=https://nitish1018-mhealth-voice-api.hf.space

# ── Backend proxy (api_server.py) ────────────────────────────────────
# Same HF Space URL, read server-side by the FastAPI proxy.
# Replaces all former Modal.com references — NEVER hardcode this value.
HF_SPACE_URL=https://nitish1018-mhealth-voice-api.hf.space
